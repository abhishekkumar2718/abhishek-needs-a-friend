Place the three user-supplied WhatsApp screenshots here:

- `form.png`: the form-sharing message.
- `embarassment.png`: the left-hand spectrum response.
- `glee.png`: the right-hand spectrum response.

Images are displayed uncropped, maintaining their original proportions. If using another format, update the corresponding `src` in `index.html`. These are genuine screenshots supplied by the presenter, not generated assets.

This repository is public. Redact any private details before committing screenshots.
